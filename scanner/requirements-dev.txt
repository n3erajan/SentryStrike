# Development / CI dependencies for the scanner worker.
# Installs the runtime deps plus the test toolchain.
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.5
pytest-asyncio==0.26.0
pytest-cov==6.1.1
